# セットアップガイド

[← ドキュメントトップへ戻る](./README.md)

---

## 📋 前提条件

- **Node.js**: v22.0.0 以上がインストールされていること（LTS推奨）
- **npm**: Node.js に付属

### Node.js バージョン確認

```bash
node -v
# v22.0.0 以上であること
```

---

## 🚀 インストール手順

### 1. リポジトリのクローン

```bash
git clone https://github.com/mikan-mikan/diff-app.git
cd diff-app
```

### 2. 依存関係のインストール

```bash
npm install
```

---

## 💻 開発サーバーの起動

ローカル開発サーバーを起動します（ホットリロード対応）：

```bash
npm run dev
```

ブラウザで以下にアクセス：

```
http://localhost:3000
```

---

## 🔧 利用可能なスクリプト

| コマンド           | 説明                        |
| ------------------ | --------------------------- |
| `npm run dev`      | 開発サーバー起動            |
| `npm run build`    | 本番用ビルド                |
| `npm run generate` | 静的サイト生成              |
| `npm run preview`  | ビルド結果のプレビュー      |
| `npm run lint`     | ESLint によるコードチェック |
| `npm run lint:fix` | ESLint による自動修正       |

---

## 📁 ローカルで静的ファイルを生成

GitHub Pages 用の静的ファイルを生成する場合：

```bash
npm run generate
```

生成されたファイルは `.output/public` ディレクトリに出力されます。

---

## ⚙️ 環境変数

| 変数名     | 説明                        | デフォルト |
| ---------- | --------------------------- | ---------- |
| `BASE_URL` | アプリケーションのベースURL | `/`        |

GitHub Pages にデプロイする場合は `BASE_URL=/diff-app/` を設定します。

---

## 🔧 ESLint 設定

このプロジェクトは **ESLint 9** の flat config 形式を使用しています。

### 設定ファイル

```
eslint.config.mjs    # ESLint flat config（新形式）
```

### 設定内容

```javascript
// @ts-check
import withNuxt from './.nuxt/eslint.config.mjs'

export default withNuxt({
  rules: {
    'no-console': 'warn',
    'no-debugger': 'warn',
    'vue/multi-word-component-names': 'off',
  },
})
```

`@nuxt/eslint` モジュールが自動的に `.nuxt/eslint.config.mjs` を生成し、Nuxt/Vue/TypeScript向けの推奨設定を提供します。
