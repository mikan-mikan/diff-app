# デプロイ

[← ドキュメントトップへ戻る](./README.md)

---

## 📌 デプロイ概要

このアプリケーションは **GitHub Pages** にホスティングされており、**GitHub Actions** による自動デプロイが設定されています。

**本番URL**: [https://mikan-mikan.github.io/diff-app/](https://mikan-mikan.github.io/diff-app/)

---

## 🔄 デプロイフロー

```
┌─────────────────┐
│  開発者がPush    │
│  (main branch)  │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ GitHub Actions  │
│   トリガー       │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  npm install    │
│  npm run generate│
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  静的ファイル生成 │
│ (.output/public) │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  GitHub Pages   │
│    デプロイ      │
└─────────────────┘
```

---

## ⚙️ 自動デプロイ

### トリガー条件

- `main` ブランチへのマージ/プッシュ

### 実行内容

1. Node.js 環境のセットアップ
2. 依存関係のインストール (`npm install`)
3. 静的サイト生成 (`npm run generate`)
4. GitHub Pages へのデプロイ

---

## 🖥️ 手動デプロイ（ローカル）

ローカル環境で静的ファイルを生成する場合：

```bash
# 静的ファイル生成
npm run generate

# 出力先
# .output/public/
```

---

## 🔧 環境変数設定

### BASE_URL

GitHub Pages にデプロイする場合、サブディレクトリでの運用となるため `BASE_URL` の設定が必要です。

| 環境         | BASE_URL     |
| ------------ | ------------ |
| ローカル開発 | `/`          |
| GitHub Pages | `/diff-app/` |

### nuxt.config.ts での設定

```typescript
app: {
  baseURL: process.env.BASE_URL || '/',
  head: {
    link: [
      {
        rel: 'icon',
        type: 'image/x-icon',
        href: `${process.env.BASE_URL}favicon.ico`,
      },
    ],
  },
},
```

---

## 📋 ビルドコマンド

| コマンド           | 用途                               |
| ------------------ | ---------------------------------- |
| `npm run build`    | サーバーサイドレンダリング用ビルド |
| `npm run generate` | 静的サイト生成（GitHub Pages用）   |
| `npm run preview`  | ビルド結果のローカルプレビュー     |

---

## ✅ デプロイ前チェックリスト

- [ ] ローカルで `npm run generate` が成功するか確認
- [ ] `npm run preview` で動作確認
- [ ] ESLint エラーがないか確認 (`npm run lint`)
- [ ] TypeScript エラーがないか確認

---

## 🐛 トラブルシューティング

### 404エラーが発生する場合

- `BASE_URL` が正しく設定されているか確認
- リソースパス（画像、CSS等）が相対パスになっているか確認

### ビルドエラーの場合

```bash
# キャッシュクリア
rm -rf .nuxt .output node_modules/.cache

# 再インストール
npm install

# 再ビルド
npm run generate
```
